[package]
name = "benchmarks-probe"
description = "Placeholder probe crate that keeps the quality gates green until the benchmark harness lands"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
